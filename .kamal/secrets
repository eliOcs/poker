# AWS ECR password (temporary token, valid 12 hours)
# In CI, password comes from environment; locally, fetch from AWS
if [ "$CI" != "true" ]; then
  KAMAL_REGISTRY_PASSWORD=$(aws ecr get-login-password --region eu-central-1 --profile personal)
fi

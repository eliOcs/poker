/**
 * Currency type representing an amount in cents (smallest currency unit).
 * Using integers for currency eliminates floating-point precision issues.
 *
 * @typedef {number} Cents
 */

// Export empty object to make this a module
export {};

version: 0.2

phases:
  install:
    commands:
      - ./tools/setup
  build:
    commands:
      - ./tools/build

artifacts:
  files:
    - frontend/build/**/*

cache:
  paths:
    - frontend/node_modules/**/*
    - backend/node_modules/**/*

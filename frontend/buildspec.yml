version: 0.2

phases:
  install:
    commands:
      - "./frontend/tools/setup"
  build:
    commands:
      - "./frontend/tools/build"

artifacts:
  base-directory: "frontend/build"
  files:
    - "**/*"

cache:
  paths:
    - "frontend/node_modules/**/*"
